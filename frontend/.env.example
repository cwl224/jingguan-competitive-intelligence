# Leave empty when using the local Vite proxy. Set this to the public FastAPI
# origin for an independently deployed frontend, without a trailing slash.
NEXT_PUBLIC_API_BASE_URL=
# Production deployments must provide a signed Bearer token issued by the
# organization identity service. Local development can leave this empty.
NEXT_PUBLIC_AUTH_TOKEN=
